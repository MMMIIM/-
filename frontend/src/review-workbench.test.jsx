import React from'react';import{renderToStaticMarkup}from'react-dom/server';import{describe,it,expect,vi,afterEach}from'vitest';import{ReviewWorkbench}from'./review-workbench.jsx';import{api}from'./api.js';
afterEach(()=>vi.restoreAllMocks());
describe('审核与控制工作台',()=>{it('未知和冲突状态保持可见，冲突批准按钮被禁用',()=>{vi.spyOn(React,'useEffect').mockImplementation(()=>{});const data={summary:{pending:2,evidence_confirmation:0,fact_confirmation:1,fact_conflict:1,risky_claims:1,missing_materials:0},pending:[{kind:'project_fact',project_fact_id:'PF-1',key:'fact.key',value:'A',status:'conflict',conflict_status:'conflict',review_status:'needs_review',fact_role:'enterprise_fact',version:1,affected_chapters:[],affected_target_count:0,mention_count:0,provenance_refs:[],reason:'事实冲突'},{kind:'claim',id:'CL-1',requirement_id:'REQ-1',text:'待审声明',status:'unknown',reason:'存在未识别状态',reason_codes:['NEW_CODE']}],evidence:[],mappings:[],claims:[],project_facts:[]};const html=renderToStaticMarkup(<ReviewWorkbench projectId="P" initialData={data}/>);expect(html).toContain('事实冲突');expect(html).toContain('未知状态（需人工核验）');expect(html).toContain('批准</button>');expect(html).toContain('disabled=""');expect(html).toContain('高级审计详情');});
 it('API 仅调用正式审核、影响预览与 Human Edit 路径',async()=>{const fetch=vi.fn(async()=>new Response(JSON.stringify({ok:true,data:{}}),{status:200,headers:{'Content-Type':'application/json'}}));vi.stubGlobal('fetch',fetch);await api.getReviewCenter('P');await api.decideEvidenceReview('ER','approve');await api.getProjectFactImpact('P','PF');await api.editProjectFact('PF',{value:'B'},'reason');expect(fetch.mock.calls.map(([u,o={}])=>[u,o.method||'GET'])).toEqual([['/api/projects/P/review-center','GET'],['/api/evidence-reviews/ER/approve','POST'],['/api/projects/P/project-facts/PF/impact','GET'],['/api/project-facts/PF/edit','POST']]);});});
