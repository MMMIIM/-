import {describe,it,expect} from 'vitest';import React from 'react';import {renderToStaticMarkup} from 'react-dom/server';import {BidDocument} from './main.jsx';
describe('标书生成状态页',()=>{it('展示门禁、风险、Sanitizer、章节重生成与版本来源',()=>{const html=renderToStaticMarkup(<BidDocument project={{id:'p',name:'项目'}} onGenerated={async()=>{}} version={{id:'v',title:'技术响应',version_number:2,risk_status:'warning',status:'pending_review',final_text:'## 数据安全与系统管理\n正文',sections_json:[{chapter_id:'chapter-06',title:'数据安全与系统管理',content_markdown:'正文'}],removed_items:[{code:'COMMERCIAL_CONTENT'}],validation_errors:[],warnings_json:[{message:'待人工复核'}]}}/>);expect(html).toContain('生成门禁与 Batch');expect(html).toContain('重生成');expect(html).toContain('Sanitizer 删除清单');expect(html).toContain('warning');expect(html).toContain('pending_review');});});
