import { createHash } from 'node:crypto';
import { AppError } from './errors.js';
import { createEvidenceFactContract, EVIDENCE_FACT_CONTRACT_VERSION, ProviderNeutralEvidenceFactExtractor } from './pipeline/evidence-fact-contract-v1.js';
const sha=(value)=>createHash('sha256').update(String(value)).digest('hex');
export class EvidenceSourceFactService {
  constructor({repository,projectAuthorizationService,extractor=new ProviderNeutralEvidenceFactExtractor(),contractVersion=EVIDENCE_FACT_CONTRACT_VERSION,extractorVersion=extractor.version||'provider-neutral-fact-stub-v1'}){this.repository=repository;this.projectAuthorizationService=projectAuthorizationService;this.extractor=extractor;this.contractVersion=contractVersion;this.extractorVersion=extractorVersion;}
  async extract({projectId,reviewId,actor}={}){if(!projectId||!reviewId||!actor)throw new AppError('FACT_PROJECT_SCOPE_REQUIRED','创建 Fact Candidate 需要明确项目范围和可信身份。',400);if(!this.projectAuthorizationService)throw new AppError('PROJECT_AUTHORIZATION_REQUIRED','项目授权服务尚未配置。',503);await this.projectAuthorizationService.assertProjectAccess({projectId,actor,action:'WRITE'});const context=await this.repository.getEvidenceReviewForFact({projectId,reviewId});if(!context)throw new AppError('EVIDENCE_REVIEW_NOT_FOUND','Evidence Review 不存在或不属于当前项目。',404);if(context.review_status!=='approved')throw new AppError('EVIDENCE_REVIEW_NOT_APPROVED','只有 approved Evidence Review 才能创建 Fact Candidate。',409);if(this.isReviewStale(context)){await this.repository.invalidateEvidenceCandidateReview(reviewId);throw new AppError('EVIDENCE_REVIEW_VERSION_INVALIDATED','Requirement、Source Span 或 Contract 已变化，旧 Evidence Review 不得继续创建 Fact。',409);}if(context.evidence_capability==='reference_only'||context.support_level==='reference_only'||context.material_type==='historical_bid')throw new AppError('EVIDENCE_FACT_REFERENCE_ONLY_FORBIDDEN','reference-only 来源不得形成正式企业 Fact Candidate。',422);const candidates=await this.extractor.extract(Object.freeze({...context}));const facts=[];for(const candidate of candidates)facts.push(await this.repository.upsertEvidenceSourceFact(createEvidenceFactContract(context,candidate,{contractVersion:this.contractVersion,extractorVersion:this.extractorVersion,extractorType:'machine'})));return{facts};}
  isReviewStale(context){return context.evidence_review_contract_version!=='evidence-review-v1'||context.source_text_hash!==context.current_source_text_hash||sha(context.source_text)!==context.current_source_text_hash;}
  async decide(factId,decision,{reviewer,note=null}={}){if(!['approve','reject'].includes(decision))throw new AppError('EVIDENCE_FACT_DECISION_INVALID','Fact 决定必须为 approve 或 reject。',422);if(!String(reviewer||'').trim())throw new AppError('EVIDENCE_FACT_REVIEWER_REQUIRED','Fact 审核人不能为空。',422);const current=await this.repository.getEvidenceSourceFactCurrent(factId);if(!current)throw new AppError('EVIDENCE_FACT_NOT_FOUND','Fact 不存在。',404);if(this.isStale(current)){await this.repository.invalidateEvidenceSourceFact(factId);throw new AppError('EVIDENCE_FACT_INVALIDATED','Fact 来源或上游 Review 已变化。',409);}if(current.review_status!=='draft')throw new AppError('EVIDENCE_FACT_ALREADY_DECIDED','Fact 已完成审核。',409);return this.repository.decideEvidenceSourceFact({factId,status:decision==='approve'?'approved':'rejected',reviewer:String(reviewer).trim(),note});}
  async edit(factId,payload,{reviewer,note=null}={}){const editor=String(reviewer||'').trim();if(!editor)throw new AppError('EVIDENCE_FACT_REVIEWER_REQUIRED','Fact 编辑人不能为空。',422);const current=await this.repository.getEvidenceSourceFactCurrent(factId);if(!current)throw new AppError('EVIDENCE_FACT_NOT_FOUND','Fact 不存在。',404);if(this.isStale(current))throw new AppError('EVIDENCE_FACT_INVALIDATED','Fact 来源或上游 Review 已变化。',409);const context={project_id:current.project_id,review_id:current.evidence_review_id,source_span_id:current.source_span_id,material_id:current.material_id,source_text_hash:current.current_source_text_hash,anchor_chunk_id:current.anchor_chunk_id,source_text:current.source_text};const edited=createEvidenceFactContract(context,payload,{contractVersion:this.contractVersion,extractorVersion:`human-edit:${editor}`,extractorType:'human',version:current.version+1,supersedesFactId:current.fact_id,editedBy:editor,editNote:note});await this.repository.invalidateEvidenceSourceFact(factId);return this.repository.upsertEvidenceSourceFact(edited);}
  isStale(current){return current.contract_version!==this.contractVersion||(current.extractor_type==='machine'&&current.extractor_version!==this.extractorVersion)||current.evidence_review_status!=='approved'||current.evidence_review_contract_version!=='evidence-review-v1'||current.source_text_hash!==current.current_source_text_hash||sha(current.source_text)!==current.current_source_text_hash;}
}
