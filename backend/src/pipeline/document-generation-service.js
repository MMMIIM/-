import { AppError } from '../errors.js';
import { buildWriterBatches,mergeWriterSections,sanitizeDocument,validateDocument,replaceChapter } from './document-generation.js';
import { chapterConfig,CHAPTER_RULE_VERSION } from './chapter-config.js';
const RULE_VERSIONS={chapters:CHAPTER_RULE_VERSION,document:'4.3-document-1',writer_contract:'4.3-section-drafting'};
async function pool(items,limit,worker){let cursor=0;const run=async()=>{while(cursor<items.length){const i=cursor++;await worker(items[i]);}};await Promise.all(Array.from({length:Math.min(limit,items.length)},run));}
export class DocumentGenerationService{
 constructor({repository,provider,concurrency=2}){this.repository=repository;this.provider=provider;this.concurrency=Math.max(1,Math.min(3,Number(concurrency)||2));}
 async gate(projectId){const input=await this.repository.getDocumentGenerationInput(projectId);if(!input.project)throw new AppError('PROJECT_NOT_FOUND','项目不存在。',404);if(!input.baseline)throw new AppError('REQUIREMENT_BASELINE_REQUIRED','请先确认 Requirement Baseline。',409);const eligible=input.requirements.filter((r)=>r.writer_eligible&&['technical','performance','implementation','delivery','service'].includes(r.requirement_category));const approved=input.claims.filter((c)=>c.decision==='approved');const covered=new Set(approved.flatMap((c)=>c.basis_requirement_ids||[]));if(eligible.some((r)=>r.is_mandatory&&!covered.has(r.req_id)))throw new AppError('COVERAGE_CRITICAL','mandatory Requirement 缺少 approved Claim，未创建正文任务。',409);return{...input,requirements:eligible,claims:approved,evidence:input.evidence.filter((e)=>e.approval_status==='approved')};}
 async generate(projectId){const input=await this.gate(projectId);const snapshot={coverage:input.coverage,requirements:input.requirements,claims:input.claims,evidence:input.evidence};const generation=await this.repository.createDocumentGeneration(projectId,snapshot,RULE_VERSIONS);const batches=buildWriterBatches({project:input.project,claims:input.claims,plans:input.plans,evidence:input.evidence});for(const batch of batches)batch.input.requirement_anchors=batch.input.response_plans.map((p)=>({requirement_id:p.requirement_id,requirement_anchor:p.requirement_anchor}));await this.repository.createDocumentTasks(generation.id,batches);await this.runBatches(generation.id,batches);return this.finalize(generation.id);}
 async runBatches(generationId,batches){await pool(batches,this.concurrency,async(batch)=>{if(!await this.repository.claimDocumentTask(generationId,batch.chapter_id,batch.batch_index))return;const started=Date.now();let attempt=0;while(attempt<2){attempt++;try{const out=await this.provider.draft(batch);await this.repository.finishDocumentTask(generationId,batch,'succeeded',{output_markdown:out.content,attempt,runtime_ms:Date.now()-started,provider_audit:out.audit});return;}catch(error){const retry=['GATEWAY_NETWORK_ERROR','GATEWAY_TIMEOUT'].includes(error.code)&&attempt<2;if(!retry){await this.repository.finishDocumentTask(generationId,batch,'failed',{attempt,runtime_ms:Date.now()-started,error_code:error.code||'WRITER_FAILED',error_message:'章节生成失败。'});return;}}}});}
 async retry(generationId){const generation=await this.repository.getDocumentGeneration(generationId);if(!generation)throw new AppError('DOCUMENT_GENERATION_NOT_FOUND','正文生成任务不存在。',404);const failed=generation.tasks.filter((t)=>t.status==='failed').map((t)=>({chapter_id:t.chapter_id,batch_index:t.batch_index,claim_ids:t.claim_ids,input:t.input_snapshot}));if(!failed.length)throw new AppError('NO_FAILED_BATCHES','没有可重试的失败 Batch。',409);await this.repository.resetDocumentTasks(generationId,failed);await this.runBatches(generationId,failed);return this.finalize(generationId);}
 async finalize(generationId){let generation=await this.repository.getDocumentGeneration(generationId);if(generation.tasks.some((t)=>t.status==='failed')){await this.repository.updateDocumentGeneration(generationId,{status:'failed',error_code:'BATCH_FAILED',error_message:'部分章节生成失败。'});return this.repository.getDocumentGeneration(generationId);}const merged=mergeWriterSections(generation.tasks);const sectionResults=merged.sections_json.map((section)=>({section,result:sanitizeDocument(section.content_markdown)}));const sanitizedSections=sectionResults.map(({section,result})=>({...section,content_markdown:result.sanitized_text}));const sanitized={sanitized_text:sanitizedSections.map((s)=>`## ${s.title}\n\n${s.content_markdown}`).join('\n\n'),removed_items:sectionResults.flatMap(({result})=>result.removed_items),requires_revision:sectionResults.some(({result})=>result.requires_revision),revision_issues:sectionResults.flatMap(({result})=>result.revision_issues)};let finalText=sanitized.sanitized_text;let revisedText=null;if(sanitized.requires_revision){const issue=sanitized.revision_issues[0];const revision=await this.provider.revise({chapter_id:generation.tasks[0]?.chapter_id,paragraph:issue.text,errors:sanitized.revision_issues,approved_claims:generation.claim_snapshot,restrictions:['不得新增事实或承诺']});revisedText=finalText.replace(issue.text,revision.content);finalText=sanitizeDocument(revisedText).sanitized_text;}const validation=validateDocument({text:finalText,requirements:generation.requirement_snapshot,approvedClaims:generation.claim_snapshot,approvedEvidence:generation.evidence_snapshot,chapterConfig});if(validation.validation_status==='critical'){await this.repository.updateDocumentGeneration(generationId,{status:'failed',risk_status:'critical',error_code:'DOCUMENT_VALIDATION_CRITICAL',error_message:'正文终检存在 critical 风险。'});return this.repository.getDocumentGeneration(generationId);}const finalSections=sanitizedSections.map((section)=>{const heading=`## ${section.title}`;const start=finalText.indexOf(heading);if(start<0)return section;const bodyStart=start+heading.length;const next=finalText.indexOf('\n## ',bodyStart);return{...section,content_markdown:finalText.slice(bodyStart,next<0?undefined:next).trim()};});const version=await this.repository.createPipelineDocumentVersion({generation,draft_text:merged.markdown,sanitized_text:sanitized.sanitized_text,revised_text:revisedText,final_text:finalText,sections_json:finalSections,validation,removed_items:sanitized.removed_items,rule_versions:RULE_VERSIONS});await this.repository.updateDocumentGeneration(generationId,{status:'finalized',risk_status:validation.validation_status});return{...(await this.repository.getDocumentGeneration(generationId)),version};}
 async regenerate(versionId,chapterId){const version=await this.repository.getPipelineDocumentVersion(versionId);if(!version)throw new AppError('VERSION_NOT_FOUND','文档版本不存在。',404);const claims=version.claim_snapshot.filter((c)=>(c.target_sections||[]).includes(chapterId));const batches=buildWriterBatches({project:{id:version.project_id,name:version.title},claims,plans:[],evidence:version.evidence_snapshot});const outputs=[];for(const batch of batches){const out=await this.provider.draft(batch);outputs.push({...batch,output_markdown:out.content});}const chapter=mergeWriterSections(outputs).sections_json.find((s)=>s.chapter_id===chapterId);if(!chapter)throw new AppError('CHAPTER_INPUT_EMPTY','目标章节没有 approved Claim。',409);const replaced=replaceChapter(version,chapterId,chapter.content_markdown);const sanitized=sanitizeDocument(replaced.markdown);const validation=validateDocument({text:sanitized.sanitized_text,requirements:version.requirement_snapshot,approvedClaims:version.claim_snapshot,approvedEvidence:version.evidence_snapshot,chapterConfig});if(validation.validation_status==='critical')throw new AppError('DOCUMENT_VALIDATION_CRITICAL','重生成后终检存在 critical 风险，未创建正式版本。',409);return this.repository.createRegeneratedVersion(version,chapterId,replaced.sections_json,sanitized,validation,RULE_VERSIONS);}
}
