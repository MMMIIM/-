import {routeRequirement} from './chapter-router.js';import {isWriterEligible} from './response-plan-validator.js';
export class DeterministicResponsePlanBuilder{
 build({requirements,approvedEvidence=[]}){const pending=requirements.filter((r)=>r.classification_review_required);if(pending.length)throw Object.assign(new Error('存在待确认分类，不能自动建立响应计划。'),{code:'CLASSIFICATION_REVIEW_REQUIRED',status:409,requirement_ids:pending.map((r)=>r.req_id)});return requirements.filter(isWriterEligible).map((r)=>{const conditions=Array.isArray(r.conditions)?r.conditions.map(String).filter(Boolean):[];const evidenceIds=approvedEvidence.filter((e)=>(e.applicable_requirement_ids||[]).includes(r.req_id)).map((e)=>e.evidence_id);return{requirement_id:r.req_id,source_status:r.source_status,confirmation_type:r.confirmation_type,requirement_category:r.requirement_category,classification_review_required:false,atomicity_review_required:Boolean(r.atomicity_review_required),response_status:'full',response_summary:'响应本项招标要求。',implementation_actions:[],optional_design:[],deliverables:[],acceptance_methods:[],conditions,supporting_evidence_ids:evidenceIds,capability_gap:'',requirement_anchor:r.text,target_sections:routeRequirement(r)};});}
}
