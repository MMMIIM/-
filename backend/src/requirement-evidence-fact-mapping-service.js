import { AppError } from './errors.js';
import { createRequirementEvidenceMapping, ProviderNeutralMappingEvaluator, REQUIREMENT_EVIDENCE_MAPPING_CONTRACT_VERSION } from './pipeline/requirement-evidence-mapping-contract-v1.js';
export class RequirementEvidenceFactMappingService {
  constructor({repository,evaluator=new ProviderNeutralMappingEvaluator(),contractVersion=REQUIREMENT_EVIDENCE_MAPPING_CONTRACT_VERSION,evaluatorVersion=evaluator.version||'provider-neutral-mapping-stub-v1'}){this.repository=repository;this.evaluator=evaluator;this.contractVersion=contractVersion;this.evaluatorVersion=evaluatorVersion;}
  async propose({projectId,requirementId,factId,sourceType='system_proposed'}){const context=await this.repository.getRequirementEvidenceFactMappingContext({projectId,requirementId,factId});if(!context)throw new AppError('MAPPING_TARGET_NOT_FOUND','Requirement 或 Evidence Fact 不存在。',404);const candidate=await this.evaluator.evaluate(Object.freeze({...context}));if(!candidate)throw new AppError('MAPPING_EVALUATION_REQUIRED','Provider-neutral evaluator 未提供 Mapping Candidate。',422);const value=createRequirementEvidenceMapping(context,{...candidate,source_type:sourceType},{contractVersion:this.contractVersion,evaluatorVersion:this.evaluatorVersion});if(this.repository.invalidateObsoleteRequirementEvidenceFactMappings)await this.repository.invalidateObsoleteRequirementEvidenceFactMappings(value);return this.repository.upsertRequirementEvidenceFactMapping(value);}
  async decide(mappingId,decision,{reviewer,note=null}={}){if(!['approve','reject'].includes(decision))throw new AppError('MAPPING_DECISION_INVALID','Mapping 决定必须为 approve 或 reject。',422);if(!String(reviewer||'').trim())throw new AppError('MAPPING_REVIEWER_REQUIRED','Mapping 审核人不能为空。',422);const current=await this.repository.getRequirementEvidenceFactMappingCurrent(mappingId);if(!current)throw new AppError('MAPPING_NOT_FOUND','Mapping 不存在。',404);if(this.isStale(current)){await this.repository.invalidateRequirementEvidenceFactMapping(mappingId);throw new AppError('MAPPING_INVALIDATED','Requirement、Fact 或版本已经变化。',409);}if(current.review_status!=='proposed')throw new AppError('MAPPING_ALREADY_DECIDED','Mapping 已完成人工决定。',409);return this.repository.decideRequirementEvidenceFactMapping({mappingId,status:decision==='approve'?'approved':'rejected',reviewer:String(reviewer).trim(),note});}
  isStale(current){return current.contract_version!==this.contractVersion||current.evaluator_version!==this.evaluatorVersion||current.current_requirement_hash!==current.requirement_hash||current.current_requirement_contract_version!==current.requirement_contract_version||current.fact_review_status!=='approved'||current.current_fact_payload_hash!==current.fact_payload_hash||current.current_fact_contract_version!==current.fact_contract_version||current.requirement_valid!==true;}
}
