import{buildWriterTask,deterministicWriterFixture,guardCriticalAssertions,materializeMentions,validateWriterOutput,verifyMentionSlices,verifyWriterPropagation}from'./pipeline/writer-execution-contract-v1.js';
export class WriterExecutionService{constructor({repository=null}={}){this.repository=repository;}buildTask(input){return buildWriterTask(input);}runDeterministic(task,options={}){const raw=deterministicWriterFixture(task),output=validateWriterOutput(task,raw,options),mentions=materializeMentions(task,output);verifyMentionSlices(output,mentions);const guard=guardCriticalAssertions(task,output),verification=verifyWriterPropagation(task,mentions,guard);output.status=guard.status==='pass'&&verification.coverage_status==='complete'?'finalized':'rejected';return{task,output,mentions,guard,verification,usage:{llm:0,dify:0,external_provider:0,writer:0}};}async persist(result){if(!this.repository)return result;const task=await this.repository.saveWriterExecutionTask(result.task),output=await this.repository.saveWriterOutput(result.output,result.guard,result.verification),mentions=await this.repository.upsertMaterializedFactMentions(result.mentions);return{...result,task,output,mentions};}async invalidateFact(factId){return this.repository.invalidateWriterExecutionByFact(factId);}}
export class ExternalWriterProvider{async generate(){const error=new Error('缺少 TASK 9D EXTERNAL WRITER AUTHORIZATION。');error.code='EXTERNAL_WRITER_AUTHORIZATION_REQUIRED';error.status=403;throw error;}}
